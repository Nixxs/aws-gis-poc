# boto3 is already present in the Lambda base image; pinning keeps it explicit.
boto3>=1.34

# DuckDB will be needed for describe-layer / query (reads parquet from S3
# directly). Uncomment when implementing those actions:
# duckdb>=1.0
